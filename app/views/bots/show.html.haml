= render 'shared/header'
= render 'shared/alert'

.container.space-top
  .col-lg-12
    %p= link_to 'Back', bots_path, class: 'btn btn-default'

    %h2{style: 'text-align: center'}= @bot.name
    %h6{style: 'text-align: center'}= "Ending message: `#{@bot.restart_msg || Bot::DEFAULT_RESTART_MSG}`"

    = render 'tab', bot: @bot
