.jumbotron
  %h2{style: 'display: inline-block'}= @bot.name
  %p Description why need set up facebook page id, and secret

  .card
    .card-body
      .paragraph
        %div
          %span Facebook page id:
          %span= @bot.facebook_page_id
        %div.ellipsis
          %span Facebook page access token:
          %span.inline-block= @bot.facebook_page_access_token

      = link_to 'Setup', edit_bot_path(@bot), class: 'btn btn-default btn-primary'
